version: '3.8'

services:
  voice-agent:
    build: .
    ports:
      - "8001:8001"
    environment:
      - PORT=8001
      # Add other environment variables here or use a .env file
      # - SUPABASE_URL=...
      # - SUPABASE_KEY=...
      # - TWILIO_ACCOUNT_SID=...
      # - TWILIO_AUTH_TOKEN=...
      # - TWILIO_NUMBER=...
      # - GEMINI_API_KEY=...
      # - NGROK_URL=... 
    restart: always
    volumes:
      - ./server_log.txt:/app/server_log.txt
      - ./error_log.txt:/app/error_log.txt
